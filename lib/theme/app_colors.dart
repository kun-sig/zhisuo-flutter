import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // 品牌主色
  static const primary = Color(0xFF2979FF);
  static const secondary = Color(0xFF5C9CFF);
  static const buttonPressed = Color(0xFF1F66E5);
  static const buttonLight = Color(0xFFE3F2FF);
  static const buttonDisabled = Color(0xFF9DBCFB);
  // 语义色
  static const success = Color(0xFF00C853);
  static const warning = Color(0xFFFFAB00);
  static const error = Color(0xFFD50000);

  // 背景层级
  static const background = Color(0xFFF8F9FA);
  static const surface = Colors.white;

  // 文本层级
  static const textPrimary = Color(0xFF1F1F1F);
  static const textSecondary = Color(0xFF6B6B6B);
  static const textDisabled = Color(0xFFBDBDBD);

  // 分割线
  static const divider = Color(0xFFE0E0E0);
}
